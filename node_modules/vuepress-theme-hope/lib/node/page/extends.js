import { injectLocalizedDate } from "vuepress-shared/node";
import { injectBlogInfo } from "./blog.js";
import { injectPageInfo } from "./info.js";
export const extendsPage = (app, page, themeData, { enableAutoExcerpt, enableBlog }, injectContentSensitiveData = false) => {
    injectLocalizedDate(page);
    injectPageInfo(page);
    if (enableBlog)
        injectBlogInfo(app, themeData, page, enableAutoExcerpt, injectContentSensitiveData);
};
//# sourceMappingURL=extends.js.map